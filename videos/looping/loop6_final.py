keys = ( 'a', 'b', 'c', 'd', 'e', 'f' )
values = ( 0, 1, 2, 3, 4 )

dictionary = dict(zip(keys, values)) # one line
    
print(dictionary) # Ta Da!

# Python is full of elegancies like this.
# The trick is not so much to know them all,
# but rather to be on the look out for them!
